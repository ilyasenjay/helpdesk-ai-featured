// Identity of the system "AI" agent user, shared between the seed script (seed-ai-agent.ts,
// which creates the row) and the inbound webhook (which looks it up to assign new tickets).
export const AI_AGENT_EMAIL = "ai-agent@helpdesk.internal";
export const AI_AGENT_NAME = "AI";
