# Sentry DSN for client-side error tracking (Sentry project → Settings → Client Keys (DSN)).
# When absent, the SDK initializes as a no-op and nothing is sent.
# VITE_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
