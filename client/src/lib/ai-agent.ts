// Mirrors server/src/lib/ai/agent.ts — kept in sync manually, same as the enum files in this
// directory mirror their Prisma counterparts.
export const AI_AGENT_EMAIL = "ai-agent@helpdesk.internal";
